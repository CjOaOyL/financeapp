Harmonia Frontend (Vite + React + TypeScript)

Quick start:

1. Install dependencies

```bash
cd frontend
npm install
```

2. Run dev server

```bash
npm run dev
```

By default the frontend will try to reach the backend at `http://localhost:3001/api`. Set `REACT_APP_API_URL` to change.
